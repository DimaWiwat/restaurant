$(document).ready(function() {
   $('.slider').slick({
      arrows:true,
      dots:true,
      adaptiveHeight:true,
      autoplay: true,
      // slidesToShow:3,
      // slidesToScroll:3,
      // speed:100,
      // easing: 'linear',
      // infinite: true,
      
   });
});